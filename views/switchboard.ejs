<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>EscapeCalifornia Switchboard</title>
  <style>
    body { font-family: Arial, sans-serif; margin: 40px; background: #f5f5f5; }
    h1 { margin-bottom: 1rem; }
    ul { list-style: none; padding: 0; }
    li { margin: 0.5rem 0; }
    a {
      text-decoration: none;
      padding: 8px 12px;
      border-radius: 4px;
      display: inline-block;
      background: #ffffff;
      box-shadow: 0 0 4px rgba(0,0,0,0.1);
    }
    a:hover { box-shadow: 0 0 8px rgba(0,0,0,0.15); }
    .tag { font-size: 0.8rem; color: #777; margin-left: 4px; }
  </style>
</head>
<body>
  <h1>EscapeCalifornia Switchboard</h1>

  <ul>
    <li>
      <a href="/">Landing / Lead Form</a>
      <span class="tag">public</span>
    </li>

    <li>
      <a href="/switchboard">Switchboard (this page)</a>
      <span class="tag">public</span>
    </li>

    <li>
      <% if (isAdmin) { %>
        <a href="/admin">Admin Panel</a>
        <span class="tag">admin (logged in)</span>
      <% } else { %>
        <a href="/admin/login">Admin Login</a>
        <span class="tag">admin (login required)</span>
      <% } %>
    </li>

    <% if (isAdmin) { %>
      <li>
        <a href="/admin/logout">Log Out of Admin</a>
        <span class="tag">admin</span>
      </li>
    <% } %>
  </ul>
</body>
</html>