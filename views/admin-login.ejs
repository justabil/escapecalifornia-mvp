<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>EscapeCalifornia Admin Login</title>
  <style>
    body { font-family: Arial, sans-serif; margin: 40px; background: #f5f5f5; }
    .box {
      max-width: 400px;
      margin: 0 auto;
      padding: 20px;
      background: #fff;
      border-radius: 8px;
      box-shadow: 0 0 8px rgba(0,0,0,0.1);
    }
    h1 { font-size: 1.5rem; margin-bottom: 1rem; }
    .error { color: #b00020; margin-bottom: 10px; }
    label { display: block; margin-bottom: 0.5rem; }
    input[type="password"] {
      width: 100%;
      padding: 8px;
      margin-bottom: 1rem;
      box-sizing: border-box;
    }
    button {
      padding: 8px 16px;
      border: none;
      border-radius: 4px;
      cursor: pointer;
    }
    .primary { background: #1976d2; color: white; }
    .link { background: transparent; color: #1976d2; text-decoration: underline; }
  </style>
</head>
<body>
  <div class="box">
    <h1>Admin Login</h1>

    <% if (error) { %>
      <div class="error"><%= error %></div>
    <% } %>

    <form method="POST" action="/admin/login">
      <input type="hidden" name="_csrf" value="<%= csrfToken %>">
      <label for="password">Admin Password</label>
      <input type="password" id="password" name="password" required />

      <button type="submit" class="primary">Log In</button>
    </form>

    <p style="margin-top: 1rem;">
      <a href="/switchboard">Back to Switchboard</a>
    </p>
  </div>

  <p style="font-size:12px;color:#999;">
  csrfToken length is: <%= csrfToken ? csrfToken.length : 0 %> |
  starts with: <%= csrfToken ? csrfToken.slice(0,10) : 'NONE' %>
</p>
  
</body>
</html>